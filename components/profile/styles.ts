
import { StyleSheet } from 'react-native';

export const profileFieldsStyles = StyleSheet.create({
  fieldsContainer: {
    paddingHorizontal: 20,
    marginTop: 40,
  },
});

export const profileFieldStyles = StyleSheet.create({
  fieldItem: {
    marginBottom: 24,
  },
  fieldValueContainer: {
    padding: 16,
    borderRadius: 12,
    marginTop: 8,
  },
  fieldLabel: {
    fontSize: 14,
    marginBottom: 8,
  },
  fieldValue: {
    fontSize: 16,
    fontWeight: '500',
  },
});